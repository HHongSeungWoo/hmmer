## 3.4-HITS 버전에서 고려 사항
1. I/O,CPU가 충분한 상황에서 현 시점 최대 병목 지점은 fasta 파일 읽기 입니다. 데이터베이스를 사용하기 전 단백질시퀀스의 줄바꿈 문자열을 제거하여 사용하는 것만으로도 성능 향상을 기대 할 수 있습니다.
2. -F, -E, -N 옵션에 따라 메모리 소모량이 달라집니다. 데이터베이스 크기의 50% 정도의 여유 메모리는 준비해야 합니다.
3. 최대 I/O 성능을 얻기 위해서는 램 디스크나 vmtouch를 사용하여 페이지 캐시에 데이터베이스를 올려놓는 것이 좋습니다.
4. 고성능 인스턴스를 사용하는 경우 하이퍼 스레딩을 끄고 사용하는 것이 도움을 줄 수 있습니다.
5. --output_limit 옵션을 사용하여 출력 크기를 조절하세요.
6. --a2m 옵션을 사용하여 출력을 a3m 형식으로 저장하세요.

## 개선 가능한 부분
1. 데이터베이스를 전처리하고 parser를 재작성할 수 있습니다. 병렬 파싱이 가능하게 구성하는 경우 I/O 병목을 제거 할 수 있습니다.
2. 램이 충분하다면 시퀀스 재사용 코드를 제거하고 모든 시퀀스를 메모리에 올리고 직접 읽어 반복 횟수 사용시 극단적인 성능 향상을 기대 할 수 있습니다.
3. AVX를 사용하도록 코드를 수정하여 성능 향상을 기대 할 수 있습니다.

## 설치
1. HITS/hmmer clone
2. hmmer 폴더 내부로 이동하여 HITS/easel clone
3. autoconf
4. ./configure --prefix 설치 경로
5. make -j<cpu>
6. make install
